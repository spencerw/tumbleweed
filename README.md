# tumbleweed
 
A browser-based tool for inspecting DCS World `.miz` files before uploading them to a server.
 
Drop a `.miz` onto the page and it will:
- List all triggers with their conditions and actions
- Group triggers by dependency chain (triggers that set flags which other triggers depend on)
- Visualize the dependency graph
